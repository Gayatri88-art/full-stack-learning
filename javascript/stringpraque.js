let msg = " hello! ";
newmsg = msg.trim().toUpperCase();
console.log(newmsg);

let name = "ApnaCollege";
 name.slice(4,9); // colle
 name.indexof("n"); //2
 name.replace("Apna","Our"); //Ourcollege


name.slice(4);
'College'
name.slice(4).replace("l","t").replace("l","t");
'Cotlege'

//arrays are mutable --> updated data your current data.

//slice-->> gives copies of an arrays.

//splice -->> use to do multiple task at the same time . (start,delete count,items to add in the array)



